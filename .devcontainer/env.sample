# PostgreSQL基本設定
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=dev_db
POSTGRES_PORT=5432

# ホスト設定
DATABASE_HOST=db              # コンテナ間通信用（バックエンド→DB）
EXTERNAL_DB_HOST=localhost   # 外部接続用（DBeaver等のGUIツール用）

# pgAdmin設定
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin